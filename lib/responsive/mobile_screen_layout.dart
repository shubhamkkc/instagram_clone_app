import 'package:flutter/material.dart';

class MobileScreenLayot extends StatelessWidget {
  const MobileScreenLayot({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("this is Mobile screen"));
  }
}
