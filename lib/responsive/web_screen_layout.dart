import 'package:flutter/material.dart';

class WebScreenLayot extends StatelessWidget {
  const WebScreenLayot({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("this is web screen"));
  }
}
